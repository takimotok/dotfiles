# 必要な file のみ zcompile する
#   $@ : 引数を全て展開
#   -nt: newer than
# https://qiita.com/GeneralD/items/73e31ab8cf9aa01e480f
function ensure_zcompiled {
  local compiled="${1}".zwc
  ([[ ! -r "${compiled}" ]] || [ "${1}" -nt "${compiled}" ]) && zcompile "${1}"
}

